# Direct dependencies, mirrored from pyproject.toml (the source of truth) and
# checked against it by scripts/export_lock.py. The installed set is
# requirements-lock.txt.
mlx-qwen3-asr[aligner]==0.3.5
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
