# Lightweight dependencies for API/unit tests on non-Apple CI runners.
# MLX is imported lazily by server.py, so model weights and GPU packages are
# not required to test the local API and transcript history.
#
# Everything shared with requirements-lock.txt is pinned to the same version,
# so a test fails here only if it would also fail for a user. httpx2 is the
# exception: it is the client Starlette's TestClient now expects, and it is
# never shipped to users.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
anyio==4.15.0
h11==0.16.0
httpx2==2.12.0
# The server now reads 16 kHz mono WAV itself rather than shelling out for
# every file, so the tests need real arrays. Pinned to the lock's version.
numpy==2.5.2
